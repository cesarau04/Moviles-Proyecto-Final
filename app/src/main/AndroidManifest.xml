<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.tec.proyectofinal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.ListJobsActivity"></activity>
        <activity android:name=".activities.CreateJobActivity" />
        <activity
            android:name=".activities.EnterpriseDashboardActivity"
            android:label="@string/title_activity_enterp_dashboard"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.UserDashboardActivity" />
        <activity
            android:name=".activities.LogInActivity" />
        <activity
            android:name=".activities.SignUpActivity"
            android:launchMode="singleInstance" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>