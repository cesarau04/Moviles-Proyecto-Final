<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.tec.proyectofinal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".JobsActivity"></activity>
        <activity android:name=".createJob" />
        <activity
            android:name=".enterpDashboard"
            android:label="@string/title_activity_enterp_dashboard"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".userDashboard" />
        <activity
            android:name=".LogIn" />
        <activity
            android:name=".SignUp"
            android:launchMode="singleInstance" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>